# Issue #4 Step 1: Display Utilities

Adds a tiny presentation-only helper module:

```text
src/hadocs/utils/display.py
```

This does not change Health Engine, generator.py, slugify(), model data, filenames, GUI or Dashboard.

It only adds helpers that later reports can use to display Home Assistant internal unassigned-area values as:

```text
No Area Assigned
```
