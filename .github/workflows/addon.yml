name: Home Assistant Add-on

on:
  push:

jobs:

  addon:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Build Add-on
        run: docker build -f hadocs/Dockerfile -t hadocs-addon ./hadocs