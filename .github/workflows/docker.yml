name: Docker

on:
  push:

jobs:

  docker:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Build image
        run: docker build -t hadocs:test .

      - name: Test image
        run: docker run --rm hadocs:test --help