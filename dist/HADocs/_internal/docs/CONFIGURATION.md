# Configuration

Example:

```json
{
  "ha_url": "http://homeassistant.local:8123",
  "token": "YOUR_TOKEN",
  "project_name": "My Smart Home",
  "output_dir": "output",
  "cache_dir": "cache"
}
```

Never commit `config.json`, `.env`, `cache/`, `output/` or generated ZIP files.
